const defaultState = {
}

//Player 1 turn on false, Player 2 turn on true
function tradeReducer(prevState = defaultState, action) {
  switch (action.type) {
    
    default:
      return prevState
  }

}

export default tradeReducer 